# ── Runtime: Core Crawler ──
pydantic>=2.0
pydantic-settings>=2.0
httpx>=0.27
curl_cffi>=0.7
tenacity>=8.0
lxml>=5.0
cssselect>=1.2
PyYAML>=6.0
motor>=3.0
minio>=7.0
aiokafka>=0.10

# ── Runtime: Web Panel (FastAPI) ──
fastapi>=0.111
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9
aiofiles>=23.2
openpyxl>=3.1
websockets>=12
anyio>=4.3

# ── Runtime: Anti-Crawl & Dedup ──
redis>=5.0

# ── Runtime: ETL Pipeline (Postgres + asyncpg) ──
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30

# ── Runtime: PDF → Markdown (Docling) ──
docling>=2.0

# ── Runtime: PDF → Markdown (PyMuPDF4LLM) ──
PyMuPDF>=1.23.0
pymupdf4llm>=0.1.0

# ── Dev / Test ──
pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=5.0
ruff>=0.8.0
mypy>=1.0
pre-commit>=3.0