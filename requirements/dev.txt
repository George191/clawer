# Development and test tools. Not installed in production Docker images.
pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=5.0
ruff>=0.8.0
mypy>=1.0
pre-commit>=3.0

# Kept for local compatibility with the previous aggregate requirements.txt.
aiologger>=0.7,<0.8
